"""ALADYN runnable examples.

Each example is a self-contained script that builds a model, runs an
analysis and (optionally) plots the results into ``examples/results/``.

Planned starter examples:

- ``pendulum_3d.py``           — single spherical pendulum.
- ``double_pendulum_spatial.py`` — spatial double pendulum.
- ``universal_joint.py``       — Cardan joint kinematics.
- ``slider_crank_3d.py``       — spatial slider-crank.
"""

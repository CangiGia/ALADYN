"""ALADYN GUI — pre/post-processor (placeholder).

Mirrors the layout of PMD's ``gui/`` package. Implementation is deferred
until the headless API is stable. The GUI must depend only on
``aladyn.solver`` and ``aladyn.io`` — never the other way around.
"""

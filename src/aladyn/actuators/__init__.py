"""Actuators — **architectural placeholder**.

Actuators are forces/torques whose magnitude is set by an external signal
(typically a control loop). They register themselves as ``forces/`` so the
EoM assembly stays uniform.
"""

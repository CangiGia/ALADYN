"""Centralized logging configuration for ALADYN.

Provides a single ``configure_logging`` entry-point. Library modules use
``logging.getLogger(__name__)`` and never call ``basicConfig`` themselves.

To be implemented.
"""

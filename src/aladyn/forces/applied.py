"""Applied forces and torques.

- ``Force``       — point force expressed in either local or global frame.
- ``Torque``      — pure torque expressed in either local or global frame.
- ``LocalForce``  — body-frame force at a marker.
- ``GlobalForce`` — global-frame force at a marker.

To be implemented.
"""

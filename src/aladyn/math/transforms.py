"""SE(3) homogeneous transforms and twist algebra.

Convenience layer used by markers and visualization. The dynamic solver
itself works directly with ``(R, p)`` pairs from ``quaternions.py``.

To be implemented.
"""

"""Spatial rigid body.

State (per body):
- centroid position ``R ∈ ℝ^3`` (global frame),
- orientation ``p ∈ ℝ^4`` (unit quaternion / Euler parameters),
- mass ``m`` (scalar),
- inertia tensor ``J ∈ ℝ^{3×3}`` expressed in the **body frame** (constant).

Reference: Shabana, *Computational Dynamics*, ch. 6.

To be implemented.
"""

"""Body-fixed reference frame (point + orientation).

A ``Marker`` is the canonical attachment point for joints and forces. It
stores its position ``s'`` and orientation ``A_m`` in the **body frame** of
its owner. Global-frame quantities are derived on demand from the body
state.

To be implemented.
"""
